<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <style>
    * {
      margin: 0px;
      padding: 0px;
    }
    body {
      display: flex;
      flex-direction: row;
    }
    .leftcontainer {
      background-color: antiquewhite;
      min-width: 130px;
      height: 100%;
    }
    .rightcontainer {
      background-color: aquamarine;
      width: 70%;
      width: 100%;
      min-height: 100vh;
    }
    /* Sidebar styles */
    .sidebar {
      width: 130px;
      height: 100vh;
      background-color: #333;
      color: white;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      position: fixed;
      top: 0;
      left: 0;
    }

    .menu {
      display: flex;
      flex-direction: column;
    }

    .menu a {
      padding: 15px 20px;
      text-decoration: none;
      color: white;
      border-bottom: 1px solid #444;
      font-size: 16px;
      transition: background-color 0.3s ease;
    }

    .menu a:hover {
      background-color: #575757;
    }

    .logout {
      padding: 15px 20px;
      text-align: center;
      color: white;
      border-top: 1px solid #444;
      background-color: #444;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .logout:hover {
      background-color: #575757;
    }

 
    .rightcontainer {
  flex: 1;
  display: flex;
  flex-direction: column;
  background-color: #2b2b2b;
}

.navbar {
  background-color: #444;
  color: #e0e0e0;
  padding: 15px;
  font-size: 1.2rem;
  text-align: center;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.5);
}

.content {
  flex: 1;
  display: flex;
  margin-top: 0.5em;
  gap: 20px;
}

.chatlist {
  flex: 1;
  background-color: #333;
  border-radius: 5px;
  overflow-y: auto;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.5);
  margin-left: 5px;
}

.chats-box-right {
  flex: 2;
  display: flex;
  flex-direction: column;
  background-color: #3a3a3a;
  border-radius: 5px;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.5);
  
}

.details {
  padding: 15px;
  background-color: #444;
  color: #e0e0e0;
  font-weight: bold;
  text-align: center;
  border-radius: 5px 5px 0 0;
}

.chats {
  flex: 1;
  padding: 15px;
  overflow-y: auto;
  background-color: #3a3a3a;
  color: #e0e0e0;
}
.chatlist a {
  text-decoration: none;
}




.chatlist ul {
  list-style: none;
  padding: 0;
  margin: 0;
 
}

.chatlist li {
  display: flex;
  align-items: center;
  padding: 10px;
  border-bottom: 1px solid #ddd;
  cursor: pointer;
  transition: background-color 0.2s;
 
  
}
.chatlist li span {
  color: #ddd;
 
}

.chatlist li:hover span {
  color: #2b2b2b; /* Ensure text color is visible on hover */

}

.chatlist li:hover {
  background-color: #f0f0f0; /* Highlight on hover */
  color: #2b2b2b;
  
}

.profile-pic {
  width: 40px; /* Adjust size */
  height: 40px;
  border-radius: 50%; /* Circular image */
  margin-right: 10px; /* Space between image and text */
  object-fit: cover; /* Ensure the image fits properly */
}

.username {
  font-size: 1rem;
  color: #333;
  font-weight: 500;
}
.chats-box-right {
    width: 300px;
    background: #1f1f1f;
    color: white;
    padding: 15px;
    border-left: 1px solid #333;
    display: flex;
    flex-direction: column;
    gap: 20px;
}

.chat-welcome {
    text-align: center;
    background: #282828;
    padding: 10px;
    border-radius: 10px;
}

.chat-suggestions h3,
.chat-emoji-box h3 {
    color: #00aaff;
    margin-bottom: 5px;
}

.chat-suggestions ul {
    list-style: none;
    padding: 0;
}

.chat-suggestions li {
    padding: 5px 0;
    font-size: 14px;
}

.chat-emoji-box {
    text-align: center;
    background: #222;
    padding: 10px;
    border-radius: 10px;
}
.sidebar a{
  text-decoration: none;
}

  </style>
  <body>
    <div class="leftcontainer">
      <div class="sidebar">
        <div class="menu">
          <a href="#home">Home</a>
          <a href="/explore">Explore</a>
          <a href="#profile">Profile</a>
        </div>
        <a href="/logout"><div class="logout">Logout</div></a>
      </div>
    </div>
    <div class="rightcontainer">
      <div class="navbar">ChatRoom</div>
      <div class="content">
        <div class="chatlist">
          <ul>
            <% users.forEach(user => { %>
              <a href="/chat/<%=user._id%>"><li class="chat-item" data-user-id="<%= user._id %>" data-username="<%= user.username %>" data-profile="<%= user.profile_pic %>">
                <img src="<%= user.profile_pic || 'default_profile_pic.jpg' %>" alt="User Profile" class="profile-pic" />
                <span class="username"><%= user.name %></span>
              </li>
            </a>
            <% }) %>
          </ul>
        </div>
        <div class="chats-box-right">
          <div class="chat-welcome">
              <h2>Welcome to ChatApp! 🚀</h2>
              <p>Connect instantly with your friends. Enjoy seamless conversations in real-time.</p>
          </div>
      
          <div class="chat-suggestions">
              <h3>🔥 Upcoming Features</h3>
              <ul>
                  <li>📌 Pin important messages</li>
                  <li>🎭 Use GIFs & Stickers</li>
                  <li>🔕 Mute notifications</li>
                  <li>💬 Start a new chat</li>
              </ul>
          </div>
      
          <div class="chat-emoji-box">
              <h3>😊 Express Yourself</h3>
              <p>Try sending: 🎉 ❤️ 😂 🤩</p>
          </div>
      </div>
      
      </div>
    </div>
  </body>
</html>
